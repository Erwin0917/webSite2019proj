import styled from 'styled-components';
import { Color } from './general/config';

export const PrimaryPageTitle = styled.h1`
	font-size: 6rem;
	font-weight: bold;
	text-shadow: 0px 3px 6px rgba(0, 0, 0, 1);
	margin-bottom: 7px;
`;

export const SecondaryPageTitle = styled.h2`
	font-size: 2.8rem;
	font-weight: regular;
	text-shadow: 0px 3px 6px rgba(0, 0, 0, 1);
	margin-bottom: 50px;
`;
