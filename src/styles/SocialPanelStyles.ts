import styled from 'styled-components';

export const SocialPanelStyles = styled.h3`
	display: flex;
	align-items: center;
	font-size: 2.2rem;
	font-weight: regular;

	.SocialPanel__icon {
		display: inline-block;
		width: 35px;
		height: 35px;
		margin: 0 10px;
	}
`;
